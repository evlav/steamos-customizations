#!/bin/sh

set -e
set -u

grubcfg=
if mountpoint -q /boot; then
    grubcfg=grub-var.cfg
else
    grubcfg=grub-root.cfg
fi

grub-mkconfig -o /efi/EFI/steamos/$grubcfg "$@"
ln -sf /efi/EFI/steamos/$grubcfg /boot/grub/grub.cfg 2>/dev/null || :
